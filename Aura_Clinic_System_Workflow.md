# 🏥 Aura Clinic - System Architecture & Workflow
**เอกสารอธิบายหลักการทำงานและสถาปัตยกรรมของระบบ Frontend และ Backend**

---

## 1. ภาพรวมสถาปัตยกรรมของระบบ (System Architecture)
ระบบ Aura Clinic ใช้โครงสร้างการพัฒนาแบบ **3-Tier Architecture** ซึ่งแบ่งการทำงานออกเป็น 3 ส่วนหลักอย่างชัดเจน เพื่อให้ง่ายต่อการดูแลรักษา (Maintenance) และการขยายระบบในอนาคต:

1. **Presentation Tier (Frontend):** ส่วนแสดงผลและติดต่อกับผู้ใช้งาน (UI) ใช้ HTML, CSS และ JavaScript
2. **Logic Tier (Backend API):** ส่วนประมวลผลตรรกะของระบบ ทำหน้าที่เป็นตัวกลางตรวจสอบเงื่อนไขและจัดการข้อมูล พัฒนาด้วย PHP
3. **Data Tier (Database):** ส่วนจัดเก็บข้อมูลระบบ ใช้ MySQL ในการเก็บข้อมูลผู้ใช้ นัดหมาย คลังสินค้า และประวัติการทำรายการ

---

## 2. หลักการทำงานฝั่ง Frontend (Client-side)
ฝั่ง Frontend หรือหน้าเว็บไซต์แอดมิน (`admin.php`, `system_tools.php`) ถูกออกแบบด้วยหลักการ **SPA (Single Page Application)** แบบประยุกต์ โดยมีกลไกสำคัญดังนี้:

* **No Page Reloads (การไม่รีโหลดหน้าเว็บ):** เมื่อมีการคลิกเมนู ค้นหาข้อมูล หรือบันทึกข้อมูล ระบบจะใช้ JavaScript ในการอัปเดตหน้าจอเฉพาะส่วนที่เปลี่ยนแปลง โดยไม่ต้องรีโหลดหน้าเว็บใหม่ทั้งหมด ทำให้ระบบทำงานได้รวดเร็วและลื่นไหล
* **Fetch API (AJAX):** ใช้คำสั่ง `fetch()` ของ JavaScript ในการส่งคำขอ (HTTP Request) แลกเปลี่ยนข้อมูลกับ Backend เบื้องหลัง โดยส่งข้อมูลไปในรูปแบบ JSON และรับข้อมูลกลับมาเป็น JSON
* **State Management เบื้องต้น:** หน้าเว็บจะมีการเก็บข้อมูลไว้ในหน่วยความจำชั่วคราว (เช่น `currentUser`, `appointmentCache`) เพื่อลดภาระการดึงข้อมูลจาก Server ซ้ำซ้อน
* **Dynamic Rendering:** ใช้ JavaScript นำข้อมูล JSON ที่ได้จาก Backend มาสร้างเป็นตาราง (HTML Table) หรือแทรกเข้าไปใน DOM แบบอัตโนมัติ

---

## 3. หลักการทำงานฝั่ง Backend (Server-side)
ฝั่ง Backend ถูกออกแบบให้เป็น **API (Application Programming Interface)** ที่เน้นความเรียบง่าย แต่มีประสิทธิภาพสำหรับการรันบนโฮสติ้งฟรี (เช่น InfinityFree)

* **Endpoint-per-Action (แยกไฟล์ตามหน้าที่):** แทนที่จะรวมโค้ดทั้งหมดไว้ในไฟล์เดียว ระบบนี้แยกไฟล์ PHP ในโฟลเดอร์ `api/` ตามจุดประสงค์ที่ชัดเจน (เช่น `appointments.php` จัดการนัดหมาย, `staff_logs.php` ดึงประวัติ) ซึ่งสอดคล้องกับหลักการ Single Responsibility Principle (SRP)
* **RESTful-like Design:** API มีการแยกลักษณะการทำงานตาม HTTP Methods:
  * `GET` : สำหรับดึงข้อมูลมาแสดงผล
  * `POST` : สำหรับเพิ่มข้อมูลใหม่ (หรือกู้คืน Backup)
  * `PUT` : สำหรับแก้ไข/อัปเดตข้อมูล
  * `DELETE` : สำหรับลบข้อมูล
* **PDO (PHP Data Objects):** ระบบเชื่อมต่อฐานข้อมูลผ่าน PDO และใช้ Prepared Statements ในทุกจุดที่มีการส่งข้อมูลเข้า Database ช่วยป้องกันการโจมตีแบบ **SQL Injection** ได้ 100%

---

## 4. หลักการจัดการความปลอดภัยและสิทธิ์ (Security & RBAC)

### 4.1 Session-Based Authentication
ระบบใช้ **PHP Session** ในการจดจำการล็อกอินของผู้ใช้
* เมื่อล็อกอินสำเร็จ Backend จะสร้างเซสชัน (`$_SESSION['user']`) ไว้ที่ฝั่ง Server 
* ทุกครั้งที่ Frontend เรียกใช้ API ตัว Backend จะเรียกใช้ฟังก์ชัน `require_auth()` เพื่อตรวจสอบว่าผู้ใช้มีเซสชันที่ถูกต้องหรือไม่ หากไม่มี API จะปฏิเสธการเข้าถึงและส่ง Status 401 (Unauthorized) ทันที

### 4.2 Role-Based Access Control (RBAC)
มีการแบ่งสิทธิ์การใช้งานออกเป็น 2 ระดับ เพื่อความปลอดภัยของข้อมูล:
* **Admin (แอดมินทั่วไป):** มีสิทธิ์จัดการข้อมูลประจำวัน เช่น เพิ่ม/ลด/แก้ไข รายการนัดหมายและคลังสินค้า (เข้าใช้งานได้เฉพาะหน้า `admin.php`)
* **Superadmin (ผู้ดูแลระบบสูงสุด):** มีสิทธิ์เข้าถึงหน้าต่าง `system_tools.php` เพิ่มเติม เพื่อใช้งานฟังก์ชัน Backup ข้อมูลทั้งระบบ, กู้คืนระบบ (Restore) และตรวจสอบประวัติการทำรายการ (Staff Logs) ของพนักงานทุกคน

---

## 5. ลำดับขั้นตอนการทำงานหลัก (Workflows)

### 🔄 Flow 1: การเข้าสู่ระบบ (Login Workflow)
1. **[Frontend]** ผู้ใช้กรอก Username / Password และกดปุ่มเข้าสู่ระบบ
2. **[Frontend]** JavaScript ดักจับ Event ตรวจสอบข้อมูลเบื้องต้น และส่งคำขอ `POST` เป็น JSON ไปยัง `api/auth_login.php`
3. **[Backend]** PHP รับค่า ทำการเข้ารหัส Password ด้วยอัลกอริทึม **SHA-256** และนำไปเทียบกับฐานข้อมูล
4. **[Backend]** หากถูกต้อง จะสร้าง Session, บันทึกลง Staff Logs ว่ามีการเข้าสู่ระบบ และส่งข้อมูล `User Object` กลับไป
5. **[Frontend]** ได้รับข้อมูลสำเร็จ จะซ่อนหน้าล็อกอิน (Login Section) และแสดงหน้าแดชบอร์ดแทน พร้อมเรียกฟังก์ชันดึงข้อมูลต่างๆ มาแสดงผล

### 🔄 Flow 2: การดึงและอัปเดตข้อมูล (Data Fetching Workflow)
1. **[Frontend]** โหลดหน้าจอเสร็จ จะยิง Request `GET` ไปที่ `api/appointments.php` และ `api/inventory.php`
2. **[Backend]** PHP ไปดึงข้อมูลจากตาราง `appointments` และ `inventory` แปลงผลลัพธ์จาก Array เป็น JSON ส่งกลับ
3. **[Frontend]** นำ JSON ไปวนลูป (Map) สร้างเป็นตารางแสดงผลบนหน้าจอ
4. **[Frontend]** เมื่อแอดมินกด "เพิ่มนัดหมาย" ระบบจะยิง Request `POST` พร้อมข้อมูลฟอร์ม
5. **[Backend]** PHP รับข้อมูลไป `INSERT` ลงตาราง พร้อมกับ `INSERT` ประวัติลง `staff_logs` และตอบกลับว่า สำเร็จ
6. **[Frontend]** เมื่อได้คำตอบว่าสำเร็จ จะโหลดข้อมูลใหม่ (Refresh) เพื่ออัปเดตหน้าจอทันที

### 🔄 Flow 3: การกู้คืนข้อมูลระบบ (System Restore Workflow)
เป็นการทำงานที่ละเอียดอ่อนและเน้นความปลอดภัยของข้อมูลขั้นสูงสุด
1. **[Frontend]** ผู้ใช้ (Superadmin) เลือกไฟล์นามสกุล `.json` ที่เคยสำรองไว้ แล้วกดปุ่มกู้คืน
2. **[Frontend]** นำไฟล์นั้นมาอ่านเป็นข้อความ (String) แปลงเป็น JSON Object และส่ง `POST` ไปที่ `api/system_restore.php`
3. **[Backend]** รับข้อมูลมา แล้วทำการตรวจสอบความถูกต้อง (Validate) ว่าไฟล์มีโครงสร้างตารางครบถ้วนหรือไม่
4. **[Backend]** หากถูกต้อง จะเริ่มกระบวนการ **Database Transaction (`BEGIN`)**:
   * ทำการ `DELETE` ข้อมูลเก่าทิ้งทั้งหมดตามลำดับที่ถูกต้อง (ป้องกัน Foreign Key Conflict ถ้ามี)
   * ทำการ `INSERT` ข้อมูลใหม่ทั้งหมดจากไฟล์ JSON ลงไปตามลำดับ
5. **[Backend]** หากทำสำเร็จทุกตาราง จะสั่ง **`COMMIT`** เพื่อเซฟข้อมูลลงฐานข้อมูลจริงอย่างถาวร แต่หากมีส่วนใดพัง หรือ Database Error ระบบจะสั่ง **`ROLLBACK`** เพื่อยกเลิกการเปลี่ยนแปลงทั้งหมด (ข้อมูลเก่าจะไม่หายไป)
6. **[Backend]** บันทึกลง Staff Logs และตอบกลับสถานะความสำเร็จให้ Frontend ทราบ



เพิ่ม logic ใน log เพิ่มขึ้นหน่อยให้เก็บข้อมูล 'เวลา , ชื่อผู้ใช้งาน , Admin Role ,  รายการ , KeyRes  ult(id)'
เวลา : เวลาในการแก้ไขหรือกระทำอะไร
ชื่อผู้ใช้งาน : ชื่อของผู้ใช้งานที่ทำการแก้ไขหรือกระทำอะไร
Admin Role : ผู้ใช้งานเป็นAdmin หรือ Superadmin
รายการ : รายการที่ทำการแก้ไขหรือกระทำอะไร เช่น เพิ่ม/ลบ/แก้ไข ข้อมูล
KeyResult(id) : id ของข้อมูลที่ทำการแก้ไขหรือกระทำอะไร (ถ้ามี)
เพิ่มช่องสำหรับใช้ค้นหาข้อมูลต่างๆใน log เช่น ค้นหาจาก id , ค้นหาจากชื่อผู้ใช้งาน , ค้นหาจากวันที่

เพิ่มความปลอดภัยให้กับหน้า login เวลาที่ใส่รหัสผิดเกิน5 ครั้ง ให้ล็อค user นั้นไว้10นาทีแล้วค่อยให้ลองใหม่ได้ และให้ยืนยัน OTP โดยระบบจะส่ง OTP ไปที่ เบอร์โทรศัพท์มือถือ ของ user นั้น แล้ว user ต้องนำ OTP นั้นมาใส่ในระบบเพื่อยืนยันตัวตน  
 
ปรับการ login ให้ด้วย Username + Password ผู้กำหนดจะเป็น Superadmin 

Superadmin: สามารถสร้างข้อมูล Username Password และ เบอร์โทรศัพท์ ให้ UserAdmin ธรรมดาได้ที่เข้าถึงได้แค่ การจัดระบบจัดการนัดหมาย และ การจัดการระบบจัดการคลังสินค้า 

UserAdmin: สามารถเข้าถึงได้แค่ การจัดระบบจัดการนัดหมาย และ การจัดการระบบจัดการคลังสินค้า


เพิ่มการ ทำ backup ระบบ อัตโนมัติทุกๆ 1 วัน เวลา 00:00 น. ทุกๆ ตารางข้อมูล
และเพิ่มฟังก์ชัน Restore ข้อมูลจากไฟล์ Backup ให้ใช้งานได้

ปรับตำแหน่งปุ่มของการเข้าหน้า SuperAdmin ให้สวยเเละกดง่ายขึ้นด้วย

